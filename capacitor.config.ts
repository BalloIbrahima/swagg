import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ib.swagg.ballo',
  appName: 'swagg',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
